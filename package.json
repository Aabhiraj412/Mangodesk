{
	"name": "mangodesk-ai-summarizer",
	"version": "1.0.0",
	"description": "AI-Powered Meeting Notes Summarizer - Full Stack MERN Application",
	"private": true,
	"type": "module",
	"scripts": {
		"dev": "concurrently \"npm run backend:dev\" \"npm run frontend:dev\"",
		"build": "bun run frontend:build",
		"start": "bun run backend:start",
		"backend:dev": "cd Backend && bun run dev",
		"backend:start": "cd Backend && bun run start",
		"frontend:dev": "cd Frontend && bun run dev",
		"frontend:build": "cd Frontend && bun run build",
		"frontend:preview": "cd Frontend && bun run preview",
		"install:all": "bun install && cd Backend && bun install && cd ../Frontend && bun install",
		"clean": "rm -rf node_modules Backend/node_modules Frontend/node_modules",
		"test": "echo \"Error: no test specified\" && exit 1",
		"build:prod": "bun run build && NODE_ENV=production bun run start",
		"deploy": "bun run build && echo \"Build completed! Run 'bun run start' to serve the production build.\""
	},
	"keywords": [
		"ai",
		"meeting-summarizer",
		"mern-stack",
		"react",
		"nodejs",
		"express",
		"mongodb",
		"groq",
		"tailwind"
	],
	"author": "MangoDesk",
	"license": "MIT",
	"devDependencies": {
		"concurrently": "^9.1.0"
	},
	"workspaces": [
		"Frontend",
		"Backend"
	],
	"engines": {
		"node": ">=18.0.0"
	}
}
