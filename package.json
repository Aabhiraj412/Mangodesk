{
	"name": "mangodesk-ai-summarizer",
	"version": "1.0.0",
	"description": "AI-Powered Meeting Notes Summarizer - Full Stack MERN Application",
	"private": true,
	"type": "module",
	"scripts": {
		"dev": "concurrently \"npm run backend:dev\" \"npm run frontend:dev\"",
		"build": "cd Frontend && npm install && npm run build",
		"start": "npm run backend:start",
		"backend:dev": "cd Backend && npm run dev",
		"backend:start": "cd Backend && npm run start",
		"frontend:dev": "cd Frontend && npm run dev",
		"frontend:build": "cd Frontend && npm run build",
		"frontend:preview": "cd Frontend && npm run preview",
		"install:all": "npm install && cd Backend && npm install && cd ../Frontend && npm install",
		"clean": "rm -rf node_modules Backend/node_modules Frontend/node_modules",
		"test": "echo \"Error: no test specified\" && exit 1",
		"build:prod": "npm run build && NODE_ENV=production npm run start",
		"deploy": "npm run build && echo \"Build completed! Run 'npm run start' to serve the production build.\""
	},
	"keywords": [
		"ai",
		"meeting-summarizer",
		"mern-stack",
		"react",
		"nodejs",
		"express",
		"mongodb",
		"groq",
		"tailwind"
	],
	"author": "MangoDesk",
	"license": "MIT",
	"devDependencies": {
		"concurrently": "^9.1.0"
	},
	"workspaces": [
		"Frontend",
		"Backend"
	],
	"engines": {
		"node": ">=18.0.0"
	}
}
